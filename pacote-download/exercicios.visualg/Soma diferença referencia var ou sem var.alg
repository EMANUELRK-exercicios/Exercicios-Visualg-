algoritmo "Soma.referencia.var.ou.sem.var"
var
   X, Y:inteiro
procedimento Soma (var A, B: inteiro)
inicio
 A <- A + 1
 B <- B + 2
   escreval(" valor de A = ", A)
   escreval(" valor de B = ", B)
   escreval(" Soma de A + B = ",A + B)
fimprocedimento
inicio
X <- 5
Y <- 9
  Soma(X,Y)
  Escreval(" valor de X = ", X)
  escreval("valor de Y = ", Y)
  


fimalgoritmo
