algoritmo "conversor"
var
   quadrada, r: real
inicio
    escreva (" informe um numero para raizq: ")
    leia (quadrada)
    r <- int(raizq (quadrada) )
    escreva (" a raiz quadrada de ", quadrada, " é exatamente ", r)

fimalgoritmo
